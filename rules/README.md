# Rules

YAML rule definitions evaluated by the engine.

- `field-hygiene/` - 38 always-on data completeness checks
- `release-lifecycle/` - 8 milestone-triggered org-policy checks

Each rule file is self-contained with condition logic, action templates,
and severity. See the top-level README for the rule schema.
